import React from 'react';
import { render } from 'react-dom';
import App from '../components/App.js';

render(<App/>, document.getElementById('app'));
//define the encompassing component, choose dom element we want to mount it to
